# 给豆包的 PPT 修订指令（v1 → v2）

> 用法：先让豆包打开已生成的《组会汇报 PPT 逐页文字稿 v1》，再整段粘贴下面的指令。

---

【修订指令】

以下是对《组会汇报 PPT 逐页文字稿 v1》的修订要求。**只修改下面列出的条目，其余页面的内容、版式规范、讲稿提示一律保持不变，不要重写未提及的页面。**

## 一、全局替换（v1 中属于编造，必须删除或改写）

1. 删除全部「TorchQuantum」字样，改为：「仿真在经典 GPU 上模拟量子线路（NVIDIA 4 GB GPU + 16 GB RAM + Intel i5，CUDA 支持），非真实量子硬件」。
2. 全文所有 `f_m^u(t) = f̂_m^u(t)`、`f^u_m = f̂^u_m` 一律改写为 `f^u_m − f̂^u_m ≥ 0`，并附一句说明：「DT 误差以 (f^u_m − f̂^u_m) 削减有效算力，这是全文数字孪生唯一介入的地方」。
3. 删除「(18a)–(18p)」「(18a)–(18q)」等写法，统一改为：「**(18b)–(18q) 共 16 条约束；(18a) 是目标函数本身，不是约束**」。

## 二、逐页修订

- **第 4 页**：补上缺失的「论文信息卡」页（v1 页码从 3 直接跳到 5）。内容：期刊 IEEE JSAC, vol. 44, pp. 5028–5042, 2026；DOI 10.1109/JSAC.2026.3700576；分区按中科院分区表 2025 年 3 月升级版为计算机科学大类 1 区、JCR Q1、IF 17.2（以学校认定版本为准）；审稿轨迹 2025-10-15 投稿 → 2026-03-23 修回 → 2026-05-30 录用 → 2026-06-05 在线；作者单位。
- **第 10 页**：删掉「存在能耗最优速度 v_tip（桨尖速度对应的最优飞行速度）」，改为「u_tip 是桨尖速度（常数）；三项式中诱导功率项随速度上升而下降，与废阻项形成权衡」。
- **第 11 页**：本地处理能耗公式改为 `E_m^proc(t) = ξ_u(1−δ_m(t))Q_m(f_m^u(t) − f̂_m^u(t))²`。
- **第 12 页**：`T_m^proc` 改为 `(1−δ_m(t))Q_m/(f_m^u(t) − f̂_m^u(t))`；总时延改为 **4 段**：AP→UAV 传输 + UAV 本地处理 + UAV→MNS 传输 + max_i{星间传输 + 从星处理}，删掉多余的 T_m^s 项。
- **第 14 页**：目标函数改写为 `f(Ω) = Σ_t w1·E^fly_u(t) + Σ_t Σ_m π_m(t)·( w2·E_m(t) + w3·T^total_m(t) )`（删掉混入的 u₂(t)，补回 π_m(t)）；约束编号按第一条改。
- **第 19 页**：QD-CE-A2C 的学习率改为 **actor 1×10⁻³、critic 1×10⁻²**（与 PPO 相同）。
- **第 21 页**：环境参数表修正两行——发射功率改为 `[p_m, p_u, p_ms,ss] = [1, 5, 5] W`；删掉「计算功率 (ξ_u, ξ_s) = (1.1, 1.1) W」，拆成两行：计算能力 `[f^max_u, f^max_s] = [1, 2] GHz`、处理能耗系数 `(ξ_u, ξ_ss) = 1×10⁻²⁷`。
- **第 22 页**：把「经典 A2C/PPO 收敛慢且最终回报低」改为「经典 A2C 收敛最慢；经典 PPO 在中后期与 QD-CE-PPO 接近，两者最终回报相近」。
- **第 23 页**：Fig. 4 横轴改为 **6 / 8 / 10 / 12**；把「QD-DRL 始终低于经典 DRL」改为「QD-DRL 在多数设置下代价更低（原文为 generally lower）」。
- **第 24 页**：把「QD-CE-A2C 对 DT 不确定性的适应性更好」改为「**QD-CE-PPO 对 DT 不确定性更鲁棒**」；括号内补原文依据：「QD-CE-A2C 在 DT 误差升高时代价受影响更大，QD-CE-PPO 展现出更强的鲁棒性与更好的整体代价表现」。本页的 `f_m^u = f̂_m^u` 按第一条改。
- **第 25 页**：删掉「注意图中只标了 AP1–AP8，而 M=10，可能有两个 AP 位置重叠或在图外」这句推测，改为「图中标注了部分 AP 位置」。
- **第 27 页**：表格第 5 行「场景无突发流量」的证据列改填「论文未给出任务到达的时间相关性模型」，我的机会列保留。不要写「任务到达为均匀分布」。
- **第 31 页**：PMEO 全称改为 **Post-Move Exact Offloading**。
- **第 36 页**：「随机突发」那一行改写——**δ=50（24.6 次，−9072.9）vs fixed τ=3（34 次，−9087.6）：同步次数更少且回报更好，这才是双赢**；δ=30（40.9 次，−8387）与 fixed τ=2（51 次，−8239）只是「更少同步、回报略低」的权衡，**禁止写成双赢**。

## 三、第 33–38 页：数字口径统一为「2026-09-09 修复版」

现稿的 k3 多机主表用的是已作废的旧版数字，必须替换。

**保持不变的（这些是对的）**：单机决策顺序增益（−81.7 / −84.1 / +2.5，37/40，p<0.0001；200 集 +2.64）；同步周期表（单机 −80.7 / −152.4 / −178.5 / −209.9 / −269.9；k3 −3568 / −5460 / −6069 / −6670 / −7565；决策一致率 1.00 → 0.62）；预测器消融（−179.1 / −178.5 / −178.7 / −178.6 / −178.9 / −186.5）；k3 突发（τ=3 34 次 −9738 vs δ=40 32.3 次 −8794，好 9.7%；τ=5 21 次 −10118 vs δ=50 26.1 次 −9048，好 10.6%）。

**k3 多机主表替换为**：

| 方法 | reward | 成功率 | 时延(s) | 能耗(J/slot) |
|---|---|---|---|---|
| PMEO-M-Eco-H5（本文） | −3594.8 | 0.8839 | 0.2334 | 364.4 |
| PMEO-M-Eco (H3) | −3644.0 | 0.8816 | 0.2341 | 351.4 |
| MPC-M-H3 | −3687.1 | 0.8795 | 0.2342 | 354.9 |
| Current-pos exact | −4193.4 | 0.8574 | 0.2429 | 456.0 |
| M-DQN | −10581.0 | 0.5823 | 0.3947 | 477.8 |
| M-TD3 | −12519.0 | 0.5311 | 0.5693 | 494.0 |
| M-SAC | −13083.6 | 0.5327 | 0.6866 | 492.3 |

结论口径：「相对 MPC-M-H3，reward / 时延 / 成功率三项系统性更优（paired p<0.001，10/10 seeds），代价是能耗 +9.5 J/slot（约 +2.7%），如实披露。」

**禁止再出现**这些旧数字：−2864.4、−2955.9、p=0.034、7/10、「能耗低 2%」、M-DQN −8658.6、Random −9052.9。

## 四、第 38–39 页

- 第 38 页「单机相对 MPC-H3 略输」保留（hard 场景 −80.7 vs −80.2），后面补一句「多机才是本方法的主战场」。
- 第 39 页 P1 列表：**删掉「给相位增益一个解析条件」**（该理论工作已完成，不能列为待办）。P1 改为三项：① LAETS 的 δ 阈值自适应标定并给出保证；② 补 LEO 可见窗口；③ DRL 基线换 MAPPO/MADDPG 重做，并给出超参搜索记录。

## 五、篇幅与页码

- 补齐第 4 页后压缩到 **32 页以内**。压缩方式：第 22、23 页合并为「实验结果：收敛性与代价趋势」一页；第 20 页复杂度分析降为第 15 页的附注；每页要点不超过 6 条。
- 页码统一改为「XX / 实际总页数」，不要再用 41 这个数字。

## 六、以下内容保持不变

配色（深蓝 #1a365d + 青色 #319795）、字体规范、公式用纯文本、图表占位符格式【此处插入论文 Fig. X】、每页的「讲稿提示」、第 24 页作为抓手页的定位、第 28 页「论文没回答的三个问题」的框架——全部保留。

请输出修订后的完整逐页文字稿，并在开头列出本次修改清单。

# Phase 3 — LAETS → 事件触发/AoI 措辞映射表（2026-09-09）

> 用途：全文机械替换 + 手工改写清单。原则：数字孪生降为“应用语境”，核心坐标挂
> NCS / AoI / event-triggered control。实现证据不改，只改口径。
> 相关代码与证据：`digital_twin_uav_leo/`（RQ1–RQ4 与 results 目录已在）。

## 1. 术语替换表

| 旧 | 新 | 说明 |
|---|---|---|
| 数字孪生（DT） | 全局状态估计的镜像（mirror）/ 孪生镜像（仅限实现名） | 不再当作方法名 |
| 孪生新鲜度 / 同步新鲜度 | 全局状态的年龄（AoI）/ 陈旧度 staleness | 挂 AoI 文献 |
| DT 同步（周期） | 固定周期全量/部分状态拉取（periodic refresh） | 有通信成本假设要写清 |
| LAETS 的“创新” | 负载漂移驱动的事件触发状态刷新 | 触发特征是负载/到达过程漂移 |
| 同步质量高 → 决策好 | 校正收益依赖状态足够新；负载陈旧比位置陈旧更伤 | 因果链第 2–3 步 |
| 突发下同步更省 | 突发下改善“刷新成本–决策质量前沿” | 不发明新范式，只做前沿 |
| 误差阈值 δ | AoI/误差阈值 δ（可映射到等效位置误差） | 沿用 adaptive_sync 定义 |

## 2. 需改写的章节位（现存文档）

- `docs/thesis_draft_pmeo_laets.md` 第 5–6 章：把“孪生同步”“新鲜度”段落按表重写，
  补 event-triggered/AoI Related Work 一段（引 NCS 经典 + 近年 AoI 综述各 1–2 条）。
- `docs/mentor_report_outline_细化.md`：标题/小节名去掉“数字孪生驱动”表述。
- `digital_twin_uav_leo/README`（若有）与 runner docstring：可保留内部命名，但
  论文/汇报文案统一用 AoI 口径。
- 论文主文件（待合并 Phase 1 新摘要后）：Introduction 第 4 段、方法第 5 章、
  Limitation —— 全部按因果链 1–4 步重写（见 plan §3.2）。

## 3. 保留不变（证据层）

- RQ1–RQ4 实验协议、taus 扫参、触发阈值 δ 扫参、突发窗口模型与数字全部保留；
- `adaptive_sync.py` 中 `estimate_error`（位置误差 + 任务漂移映射）语义不变；
- 只改“我们发明了 DT”为“我们在 AoI/事件触发坐标下把刷新策略用于空天地卸载决策
  质量（指标是 MEC reward，不是纯控制稳定）”。

## 4. 鲁棒实验（已完成，§4.5 RQ7）

- ✅ 同步延迟 2 时隙 / 刷新丢包 10% 下**在每种损伤内重扫固定周期前沿**并重跑本地触发器；
- ✅ 报告“事件触发是否仍支配**同条件**的前沿”：三种条件下均仍支配，但领先幅度由 +224 收窄至
  +124（延迟）与 +33（丢包，落在此样本量的统计噪声内）；
- ✅ 零延迟/无丢包不再只是 Limitation 里的假设，而有了**同条件对照**的结果与边界声明。
- 未做（可选）：触发阈值 δ 扰动扫描。
